apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - configmap.yaml

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: metronome-simulator
    path: deployment-patch.yaml